import { GuildMember } from "discord.js";

/**
 * Envoie la cible au goulag. **Ne vérifie pas le niveau de hierarchie entre la cible et le commanditaire**.
**/
export async function envoyerGoulag(cible:GuildMember, commanditaire : GuildMember, motif?: string | null, duree ?: number | null) {
}
